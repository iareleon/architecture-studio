# Project Memory — SkillsLoom

## Conventions

- Skill directory names always use kebab-case (e.g. `my-skill`, not `mySkill` or `my_skill`).
- Always use `target-folder:` as the field label for destination paths — never `target-dir` or `target-sub-path`.
