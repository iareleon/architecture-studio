# Deferred skill plans

When a user defers a skill idea, store a short stub here:

`{skill-slug}-YYYY-MM-DD.md`

Referenced from the `skill-manager` and `brain-manager` workflows instead of a top-level `brain-manager/deferred-skills/` tree.
