# Deferred skill plans

When a user defers a skill idea, store a short stub here:

`{skill-slug}-YYYY-MM-DD.md`

Referenced from the `skill-manager` and memory workflows instead of a top-level `memory/deferred-skills/` tree.
