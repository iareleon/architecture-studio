# React / front-end UI persona

- **Stack:** React 19, TypeScript, Tailwind CSS.
- **Conventions:** prefer Server Components and Server Actions where applicable; strict WCAG 2.2; mobile-first layout; no `any`; use Zod or similar at boundaries when you need runtime validation.

Load when the work is browser UI, components, or front-end build tooling—not Python backends.
